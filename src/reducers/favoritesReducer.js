


const initialState = {
    favorites: [],
    displayFavorites: false
}

const reducer = (state = initialState, action) => {
    switch(action.type) {
        // case ADD_FAVORITE:
        //     return { ...state,
                
        //     }
        default:
            return state;
    }
}

export default reducer;